export const Navbar = ({children}) => {
  return (
    <div className="navbar bg-base-200">
       <a className="btn btn-ghost normal-case text-xl" href="https://github.com/Riiz08">{children}</a>
     </div>
    )
}